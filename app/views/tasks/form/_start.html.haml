= form_for(task) do |f|
  = f.hidden_field :started_at, value: Time.now.to_s(:db)
  = f.hidden_field :status, value: 'doing' 
  = f.hidden_field :week_id, value: nil
  = f.button " #{content_tag(:i, '', class: 'icon-bone')}START".html_safe, type: :submit
